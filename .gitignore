# 个人配置（密钥）永不入库 —— 唯一合法位置是用户配置目录：
#   Windows: %APPDATA%\scrader_mcp\config.json   其他: ~/.config/scrader_mcp/config.json
mcp-server/providers.json
config.json

node_modules/
*.log
.DS_Store

# 运行期产物
chrome_state.json

# 打包产物
dist/
