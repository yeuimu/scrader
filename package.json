{
  "name": "scrader-mcp",
  "version": "0.4.1",
  "description": "scrader (spider + scraper) — browser operations & web scraping exposed as MCP tools for any AI Agent: trusted humanized input, generic list harvesting (harvest), optional Jev fast decisions",
  "license": "MIT",
  "bin": { "scrader-mcp": "mcp-server/scrader-mcp.js" },
  "files": [
    "mcp-server/scrader-mcp.js",
    "mcp-server/bridge.js",
    "mcp-server/providers.example.json",
    "README.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "engines": { "node": ">=18" },
  "keywords": ["mcp", "chrome-extension", "scraper", "browser-automation", "harvest", "jev"],
  "scripts": {
    "check": "node mcp-server/scrader-mcp.js --check",
    "selftest": "node mcp-server/selftest.js"
  }
}
